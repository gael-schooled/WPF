<Window x:Class="WPF_Project.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Project"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="White">
            <!-- Menu Communication -->
            <MenuItem Header="Communication">
                
                <MenuItem Header="Listener/Client">
                    <MenuItem Header="Ecouter" Click="ListenerEcouter_Click"/>
                    <MenuItem Header="Connecter" Click="ListenerConnecter_Click"/>
                </MenuItem>

                <MenuItem Header="UDP">
                    <MenuItem Header="Ecouter" Click="UDPEcoluter_Click"/>
                    <MenuItem Header="Connecter" Click="UDPConnecter_Click"/>
                </MenuItem>

                <MenuItem Header="Socket">
                    <MenuItem Header="Ecouter" Click="SocketEcouter_Click"/>
                    <MenuItem Header="Connecter" Click="SocketConnecter_Click"/>
                    <Separator/>
                    <MenuItem Header="Déconnecter" Click="SocketDeconnecter_Click"/>
                </MenuItem>
            </MenuItem>

            <MenuItem Header="Utilitaire">
                <MenuItem Header="Vérifier" Click="Verifier_Click"/>
            </MenuItem>
        </Menu>

        <TextBlock Grid.Row="1"  Text="Serveur :" VerticalAlignment="Center" Margin="1"/>
        <TextBox Grid.Row="2"  x:Name="Server" Text="" Margin="1"/>

        <TextBlock Grid.Row="3"  Text="IP :" VerticalAlignment="Center" Margin="1"/>
        <TextBlock Grid.Row="4"  x:Name="IP" Text="XX.XX.XX.XX" VerticalAlignment="Center" Margin="1"/>
        
        <TextBlock Grid.Row="5"  Text="Message :" VerticalAlignment="Center" Margin="1"/>
        <TextBox Grid.Row="6"  x:Name="Message" Text="" Margin="1"/>

        <Button Grid.Row="7" Grid.Column="1" x:Name="Envoyer" Margin="5,5,5,5" Click="Envoyer_Click">
            <Button.Content >
                Envoyer
            </Button.Content>
        </Button>

        <TextBlock Grid.Row="8"  Text="Echanges :" VerticalAlignment="Center" Margin="1"/>

        <TextBox Grid.Row="9" Grid.Column="1" x:Name="Echanges" Margin="5,5,5,5" Height="265" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        
    </Grid>
</Window>
